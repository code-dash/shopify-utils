const state = {
  csrfToken: 'test'
};

const getters = {
  csrfToken: state => state.csrfToken
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
}